- name: create a file
  hosts: all
  become: yes

  tasks:
    - name: create a directory
      file:
         name: test-dir
         path: /root/
         state: directory
         mode: '0777'

    - name: createingfile
      file:
      path: /root/testfile1
      state: touch          